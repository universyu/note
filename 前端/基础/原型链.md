
所有的函数有一个属性 prototype ，用函数构造出来的示例有一个属性 `__proto__` 它和构造函数的 prototype 是同一个对象实例。 `Object.prototype.__proto__` 是 null ，而所有的函数都是 Function 的实例，所以 `Object.__proto__ = Function.prototype`  又因为 proto 和 prototype 都是对象实例，是用 Object 构造的，所以 `Function.prototype.__proto__ = Object.prototype`